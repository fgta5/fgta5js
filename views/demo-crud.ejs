<!-- demo-crud
genearted by fgta5js application generator 
2025-06-18
Agung Nugroho DW

DO NOT directly edit this file
this content will be replaced when regenerated
--><!DOCTYPE html>
<html>
	<head>
		<title><%= title %></title>
		<meta name="viewport" content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
		<meta http-equiv="X-UA-Compatible" content="IE=edge">
		<%- include(style); %>
		<%- include(script); %>
		<link rel="stylesheet" href="./styles/demo.css" />
		<link rel="stylesheet" href="./styles/demo-application.css" />
		<style>
			#loadingindicator {
				position: fixed; top: 0; left: 0; right: 0; bottom: 0; display: flex; justify-content: center; align-items: center; background-color: #fff;
				transition: opacity 0.5s ease, visibility 0.5s ease;
			}
		</style>
	</head>
	<body>

		<!-- Loading Indicator -->
		<div id="loadingindicator">
			<div style="font-family: system-ui, sans-serif">
				loading demo crud module...
			</div>
		</div>

		<!-- Main Application Layout -->
		<main id="myapp" class="hidden">
			<div data-panel name="panel1">
				panel 1	
			</div>	
			<div data-panel name="panel2">
				panel 2		
			</div>	
			<div data-panel name="panel3">
				panel 3		
			</div>	
			<div data-action name="action1">
				<button>save</button>	
				<button>new</button>	
				<button>delete</button>	
			</div>	
		</main>	


	</body>

	<% if (env=='release') { %>
		<!-- RELEASE MODE -->
		<script type="module">
			import MyApp from './dist/demo-crud-v1.0.0-min.js'
			window.addEventListener("load", async (event) => {
				var $app = new MyApp();
				await $app.main();

				// remove loading indikator
				var myapp = document.getElementById('myapp')
				var pagemask = document.getElementById('loadingindicator')
				myapp.classList.remove('hidden')
				setTimeout(()=>{
					pagemask.style.opacity = 0
					pagemask.style.visibility = 'hidden'
					setTimeout(()=>{
						pagemask.parentNode.removeChild(pagemask)
					}, 500)
				}, 500)
			});
		</script>	
	<% } else { %>
		<!-- DEBUG MODE  -->
		<script type="module">
			import MyApp from './src/demo-crud.mjs'
			window.addEventListener("load", async (event) => {
				var $app = new MyApp();
				await $app.main();

				// remove loading indikator
				var myapp = document.getElementById('myapp')
				var pagemask = document.getElementById('loadingindicator')
				myapp.classList.remove('hidden')
				setTimeout(()=>{
					pagemask.style.opacity = 0
					pagemask.style.visibility = 'hidden'
					setTimeout(()=>{
						pagemask.parentNode.removeChild(pagemask)
					}, 500)
				}, 500)			
			});
		</script>	
	<% } %>	

</html>			